/**
 * TypeScript environment declarations for Vite.
 *
 * References Vite's client type definitions, enabling type-checking
 * for Vite-specific globals like `import.meta.env`, `.vue` file imports,
 * and static asset imports (CSS, images, etc.).
 */
/// <reference types="vite/client" />
